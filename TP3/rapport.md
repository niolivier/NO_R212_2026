1. Le "bouton enregistrer" du navigateur
J'ai appris à utiliser le localStorage comme une base de données locale. C'est grâce à la commande setItem qu'on peut forcer le navigateur à noter nos projets dans sa mémoire. Ce qui est top, c'est que même si je ferme l'onglet ou que j'éteins mon ordi, mes projets sont toujours là quand je reviens.

2. Le filtrage dynamique (Exercice 2)
J'ai retenu qu'on pouvait manipuler l'affichage sans recharger la page. En utilisant .filter() sur mon tableau, je peux créer une liste temporaire qui ne contient que les projets avec un certain "tag". C'est ce qui permet de passer de "Tous" à "JS" ou "CSS" en un clic de manière instantanée.

"Aucune IA utilisée lors de cette séance." -->